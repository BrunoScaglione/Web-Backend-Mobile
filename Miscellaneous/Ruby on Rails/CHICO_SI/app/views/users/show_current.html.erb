<h1>Usuário</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user %>
  </div>
</div>
<p>Email: <%= @user.email %></p>
<p>Autoridade: <%= @user.authority %></p>
<% if @user.groups.any? %>
  <p>Grupos: <%= render @user.groups %></p>
<% end %>

<%= link_to 'Editar', edit_user_path(@user) %><br/>
<%= link_to 'Home', home_path %>