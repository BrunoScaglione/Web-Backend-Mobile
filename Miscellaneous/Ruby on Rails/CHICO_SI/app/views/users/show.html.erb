<h1>Usuário</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user %>
  </div>
</div>
<p>Email: <%= @user.email %></p>
<p>Autoridade: <%= @user.authority %></p>
<% if @user.groups.any? %>
  <p>Grupos: <%= render @user.groups %></p>
<% end %>

<% if current_user.authority == 2 && current_user.id != @user.id %>
  <%= link_to 'Deletar', @event, method: :delete, data: { confirm: 'Tem certeza?' } %>
<% end %>
<%= link_to 'Editar', edit_user_path %><br/>
<%= link_to 'Voltar', users_path %><br/>
<%= link_to 'Home', home_path %>