<%= form_with(model: plano, local: true) do |f| %>
  <% if plano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plano.errors.count, "error") %> impediu este plano de ser criado:</h2>

      <ul>
        <% plano.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit 'Salvar'%>
  </div>
<% end %>